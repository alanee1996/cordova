var loginFunction = {
  init: function (obj) {
    $('#loginForm').submit(function () {
      var model = {
        email: $('#login_email').val(),
        password: $('#login_password').val()
        }
        
    })
  }
}
